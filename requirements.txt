# Amyloid Aggregation Database Pipeline
# Install: pip install -r requirements.txt

# HTTP requests
requests>=2.28.0

# LLM providers (choose one or more)
anthropic>=0.18.0
openai>=1.0.0
# Ollama - no pip package needed, just install from ollama.ai

# NER / NLP (CPU-friendly)
spacy>=3.5.0
scispacy>=0.5.4

# scispaCy models - install separately:
# pip install https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz

# PubMedBERT NER (optional, higher quality, slower on CPU)
transformers>=4.30.0
torch>=2.0.0  # CPU version: pip install torch --index-url https://download.pytorch.org/whl/cpu

# Data processing
pandas>=2.0.0

# Progress bars
tqdm>=4.65.0

# Testing
pytest>=7.0.0
